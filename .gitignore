# sdkcraft build artifacts
*.sdk
# spread local state
tests/.spread-reuse.yaml
# staged try SDKs for local spread runs (populated by: cp -r ~/.local/share/workshop/try/headroom-client tests/sdks/)
tests/sdks/
